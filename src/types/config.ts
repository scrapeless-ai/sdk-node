export interface ScrapelessConfig {
  baseApiUrl?: string;
  actorApiUrl?: string;
  storageApiUrl?: string;
  browserApiUrl?: string;
  scrapingCrawlApiUrl?: string;

  apiKey?: string;
  timeout?: number;
}
